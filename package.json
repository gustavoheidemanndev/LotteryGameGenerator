{
  "name": "python-fastapi-math",
  "private": true,
  "scripts": {
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 3000 --reload",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-deps": "python -m pip install --upgrade pip && python -m pip install -r requirements.txt"
  },
  "dependencies": {
    "httpx": "^3.0.1"
  }
}
